# ============================================
# PIS 完全自托管环境变量配置
# ============================================
#
# 🚀 推荐使用一键部署脚本（自动生成此文件）:
#    cd docker && bash deploy.sh
#
# 手动配置说明:
# 1. 复制此文件为 .env（注意：是 .env 不是 .env.standalone）
# 2. 修改所有标有 "请修改" 的配置项
# 3. cd docker && docker compose -f docker-compose.standalone.yml up -d
#
# 注意: 此配置不使用 Supabase，所有服务自托管
# ============================================

# ==================== 域名配置 ====================
# 你的主域名（用于生成访问 URL）
DOMAIN=yourdomain.com

# ==================== 数据库配置 ====================
# 使用直连 PostgreSQL 模式（不依赖 Supabase）
DATABASE_TYPE=postgresql
POSTGRES_DB=pis
POSTGRES_USER=pis
# ⚠️ 请修改为强密码（至少 16 字符）
POSTGRES_PASSWORD=changeme-postgres-password-please-change

# ==================== 存储配置 (MinIO) ====================
# MinIO 访问密钥（用于 API 访问）
# ⚠️ 请修改为强密码（至少 16 字符）
MINIO_ACCESS_KEY=your-minio-access-key
MINIO_SECRET_KEY=your-minio-secret-key-at-least-16-chars
MINIO_BUCKET=pis-photos

# 存储公网 URL（用于前端访问图片）
# 格式: https://你的域名/media
STORAGE_PUBLIC_URL=https://yourdomain.com/media
MINIO_PUBLIC_URL=https://yourdomain.com/media

# ==================== Supabase 配置（前端认证必需）====================
# ⚠️ 前端认证需要 Supabase，请在 https://supabase.com 创建免费项目
# 进入 Settings → API 获取以下信息
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ==================== Worker 配置 ====================
# Worker API 密钥（用于 API 认证）
# ⚠️ 请修改为随机字符串（至少 32 字符）
# 生成方式: openssl rand -hex 32
WORKER_API_KEY=your-worker-api-key-please-change-at-least-32-chars

# Worker 服务 URL（内部网络，通常不需要修改）
WORKER_URL=http://worker:3001

# ==================== 应用配置 ====================
# 应用访问地址
NEXT_PUBLIC_APP_URL=https://yourdomain.com
# 媒体文件访问地址
NEXT_PUBLIC_MEDIA_URL=https://yourdomain.com/media
# Worker 服务地址（前端使用）
NEXT_PUBLIC_WORKER_URL=https://yourdomain.com/worker-api

# 品牌信息
NEXT_PUBLIC_PHOTOGRAPHER_NAME=PIS Photography
NEXT_PUBLIC_PHOTOGRAPHER_TAGLINE=专业活动摄影

# ==================== 安全配置（必须设置）====================
# 相册会话密钥（用于 JWT Token）
# ⚠️ 请修改为随机字符串（至少 32 字符）
# 生成方式: openssl rand -hex 32
ALBUM_SESSION_SECRET=your-album-session-secret-at-least-32-chars

# ==================== 告警服务（可选）====================
ALERT_ENABLED=true
ALERT_TYPE=log

# Telegram 告警（可选）
# TELEGRAM_BOT_TOKEN=your-bot-token
# TELEGRAM_CHAT_ID=your-chat-id

# 邮件告警（可选）
# ALERT_SMTP_HOST=smtp.gmail.com
# ALERT_SMTP_PORT=587
# ALERT_SMTP_USER=your-email@gmail.com
# ALERT_SMTP_PASS=your-app-password
# ALERT_FROM_EMAIL=noreply@yourdomain.com
# ALERT_TO_EMAIL=admin@yourdomain.com

# ==================== Cloudflare（可选）====================
# 如果使用 Cloudflare CDN，配置以下变量
# CLOUDFLARE_API_TOKEN=your-api-token
# CLOUDFLARE_ZONE_ID=your-zone-id
# CLOUDFLARE_MEDIA_DOMAIN=yourdomain.com

# ==================== 图片处理配置（可选）====================
# 预览图最大尺寸（默认 1920px）
# PREVIEW_MAX_SIZE=1920
# 缩略图最大尺寸（默认 400px）
# THUMB_MAX_SIZE=400

# ==================== Node.js 配置 =====================
NODE_ENV=production
LOG_LEVEL=info

# ==================== 端口绑定（可选）====================
# Worker 绑定地址（默认 127.0.0.1，仅本地访问）
# WORKER_BIND_HOST=127.0.0.1
