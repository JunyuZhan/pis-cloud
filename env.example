# ============================================
# PIS 环境变量配置模板
# 
# 前端 (apps/web): 复制为 .env.local
# Worker (docker): 复制为 .env
# ============================================

# ==================== Supabase ====================
# 从 Supabase Dashboard -> Settings -> API 获取

# 项目 URL (前端+后端共用)
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co

# 公开密钥 (仅前端使用，可安全暴露)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# 服务端密钥 (仅 Worker 使用，必须保密！)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ==================== MinIO ====================
# Docker 内部使用
MINIO_ENDPOINT_HOST=minio
MINIO_ENDPOINT_PORT=9000
MINIO_USE_SSL=false

# 前端 API 使用 (Vercel)
# MINIO_ENDPOINT=media.yourdomain.com
# MINIO_PORT=443
# MINIO_USE_SSL=true

# 凭证 (自定义强密码，至少 8 位)
MINIO_ACCESS_KEY=your-access-key
MINIO_SECRET_KEY=your-secret-key-min-8-chars

# Bucket 名称
MINIO_BUCKET=pis-photos

# ==================== Redis ====================
# Docker 内部网络使用
REDIS_HOST=redis
REDIS_PORT=6379

# ==================== 应用配置 ====================
# 主站域名
NEXT_PUBLIC_APP_URL=https://yourdomain.com

# 媒体 CDN 地址 (MinIO 反向代理)
# 注意: 需要包含 bucket 路径
NEXT_PUBLIC_MEDIA_URL=https://media.yourdomain.com/pis-photos

# ==================== Node 环境 ====================
NODE_ENV=production
