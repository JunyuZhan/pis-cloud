# React 安全漏洞警报

**更新日期**: 2026-01-26  
**严重程度**: 🔴 严重

---

## 🚨 关键漏洞：React Server Components (CVE-2025-55182)

### 漏洞概述

**CVE-2025-55182 (React2Shell)** 是一个**严重级别**的远程代码执行（RCE）漏洞，影响 React Server Components。

**CVSS 评分**: 10.0 (Critical)

### 受影响版本

- **React**: 19.0.0, 19.1.0, 19.1.1, 19.2.0
- **Next.js**: 14.3.0-canary 及更高版本, 15.x, 16.x (使用 App Router)
- **其他框架**: Vite, Parcel, React Router, RedwoodSDK, Waku (使用 React Server Components)

### 漏洞详情

- **类型**: 预认证远程代码执行（RCE）
- **原因**: Server Function 端点中 HTTP 请求负载的不安全反序列化
- **披露时间**: 2025年12月3日
- **活跃利用**: 2025年12月5日发现活跃利用，估计有 60,000 台易受攻击的服务器暴露在公网

### 修复版本

- **React**: 19.0.3, 19.1.4, 19.2.3
- **Next.js**: 15.0.5, 15.1.9, 15.2.6, 15.3.6, 15.4.8, 15.5.7, 16.0.7

---

## ⚠️ 相关漏洞

### CVE-2025-55183
- **类型**: 信息泄露
- **影响**: 暴露服务器端源代码和凭据

### CVE-2025-55184 & CVE-2025-67779
- **类型**: 拒绝服务（DoS）
- **影响**: React Functions 中的 DoS 漏洞

### Node.js 栈溢出问题（2026年1月）

- **问题**: 当 `async_hooks` 启用时（React Server Components、Next.js 和 APM 工具使用），栈溢出错误会导致 Node.js 立即退出
- **影响**: 可能导致应用程序意外崩溃
- **建议**: 尽快更新 Node.js

---

## 🔍 项目检查

### ⚠️ 项目当前状态

**你的项目受影响！**

- ✅ **React**: `^19.0.0` - **受影响**（需要更新到 19.0.3+）
- ✅ **Next.js**: `^15.0.0` - **受影响**（需要更新到 15.0.5+）
- ✅ **使用 App Router**: 项目使用 Next.js App Router，默认使用 React Server Components

### 检查当前版本

```bash
# 检查 React 版本
cd apps/web
npm list react react-dom

# 检查 Next.js 版本
npm list next
```

### 为什么受影响？

你的项目使用了：
- Next.js 15.x App Router（默认使用 React Server Components）
- React 19.0.0（受影响的版本）
- Server Components（`async function` 组件在 `app/` 目录中）

---

## ✅ 修复步骤

### 1. 立即更新 React（必须）

```bash
cd apps/web

# 更新到安全版本（推荐使用 19.2.3，最新稳定版）
npm install react@^19.2.3 react-dom@^19.2.3

# 或者使用 caret 范围（会自动安装最新补丁版本）
npm install react@^19.2.0 react-dom@^19.2.0
```

### 2. 立即更新 Next.js（必须）

```bash
cd apps/web

# 更新到安全版本（推荐使用 15.5.7，最新稳定版）
npm install next@^15.5.7

# 或者使用 caret 范围（会自动安装最新补丁版本）
npm install next@^15.5.0
```

### 3. 验证更新

```bash
# 检查版本是否正确
npm list react react-dom next

# 应该看到：
# react@19.2.3 (或更高)
# react-dom@19.2.3 (或更高)
# next@15.5.7 (或更高)
```

### 3. 更新 Node.js

确保使用最新版本的 Node.js（建议 Node.js 20.x LTS 或更高版本）。

### 4. 检查依赖

```bash
# 检查是否有易受攻击的依赖
npm audit

# 自动修复（如果可能）
npm audit fix
```

---

## 📋 验证清单

- [x] ✅ 检查项目使用的 React 版本 - **React 19.0.0（受影响）**
- [x] ✅ 检查项目使用的 Next.js 版本 - **Next.js 15.0.0（受影响）**
- [x] ✅ 确认是否使用了 React Server Components - **是（使用 App Router）**
- [ ] ⚠️ **立即更新到修复版本** - **必须立即执行**
- [ ] 运行 `npm audit` 检查其他依赖漏洞
- [ ] 测试更新后的应用程序
- [ ] 部署更新到生产环境

## 🚨 紧急行动

**你的项目使用了受影响的版本，需要立即更新！**

### 快速修复命令

```bash
cd apps/web

# 一次性更新所有受影响的包
npm install react@^19.2.3 react-dom@^19.2.3 next@^15.5.7

# 验证更新
npm list react react-dom next

# 运行测试
npm run build
npm run dev  # 测试开发环境
```

---

## 🔗 参考链接

- [CVE-2025-55182 详情](https://nvd.nist.gov/vuln/detail/CVE-2025-55182)
- [React2Shell 官方页面](https://react2shell.com/)
- [Vercel 安全公告](https://vercel.com/changelog/cve-2025-55182)
- [React GitHub 安全公告](https://github.com/facebook/react/security/advisories)
- [Next.js GitHub 安全公告](https://github.com/vercel/next.js/security/advisories)

---

## ⚠️ 重要提醒

1. **立即行动**: 如果使用 React Server Components，这是严重漏洞，需要立即修复
2. **扫描环境**: 扫描所有环境以识别易受攻击的应用程序
3. **监控日志**: 检查是否有异常活动或利用尝试
4. **备份**: 更新前备份代码和数据库

---

**最后更新**: 2026-01-26
